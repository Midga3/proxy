"""
Файл конфигурации для Telegram SOCKS5 Proxy
Измените параметры по своему усмотрению
"""

# URL списка SOCKS5 прокси
PROXY_LIST_URL = "https://raw.githubusercontent.com/hookzof/socks5_list/refs/heads/master/tg/socks.json"

# Максимальный пинг в миллисекундах
# Прокси с пингом выше этого значения будут отфильтрованы
MAX_PING = 300

# Адрес для прослушивания локального SOCKS5 сервера
# 127.0.0.1 - только локальные подключения (рекомендуется)
# 0.0.0.0 - все подключения (НЕБЕЗОПАСНО для публичных сетей!)
LOCAL_HOST = "127.0.0.1"

# Порт локального SOCKS5 сервера
LOCAL_PORT = 1080

# Интервал обновления списка прокси (в секундах)
# 600 секунд = 10 минут
UPDATE_INTERVAL = 600

# Размер буфера для передачи данных (в байтах)
BUFFER_SIZE = 8192

# Таймауты (в секундах)
CONNECTION_TIMEOUT = 10  # Таймаут подключения к upstream прокси
CLIENT_TIMEOUT = 10      # Таймаут ожидания данных от клиента
SOCKS_TIMEOUT = 5        # Таймаут SOCKS5 handshake

# Логирование
# True - включить подробное логирование (все ошибки)
# False - минимальное логирование (только важные события)
# Рекомендуется False для уменьшения спама в логах
VERBOSE = True

# Фильтр по странам (необязательно)
# Если список не пустой, будут использоваться только прокси из указанных стран
# Примеры кодов стран: "US", "DE", "FR", "NL", "SG", "AT"
# Пустой список [] означает использование прокси из всех стран
ALLOWED_COUNTRIES = []

# Исключить определенные страны
# Прокси из этих стран не будут использоваться
EXCLUDED_COUNTRIES = []

# Минимальное время работы прокси (addTime) в секундах
# Прокси, добавленные в список менее этого времени назад, могут быть нестабильными
# 0 - не фильтровать по времени
MIN_PROXY_AGE = 0

